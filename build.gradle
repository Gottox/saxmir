group = 'de.gottox'
description = 'CSS selectors for SAX'
version = 'master'

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.10'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.8'
}
